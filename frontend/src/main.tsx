import React from 'react';
import ReactDOM from 'react-dom/client';

// Minimal entry point for build
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>Comicarr - Minimal Setup</div>
  </React.StrictMode>,
);
